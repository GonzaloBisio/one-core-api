package com.one.core.domain.model.enums.gym;

// Access rules del plan
public enum GymAccessMode {
    UNLIMITED, // Acceso ilimitado
    N_PER_WEEK, // N accesos por semana
    N_PER_MONTH // N accesos por mes
}
